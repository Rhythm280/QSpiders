import React, { useMemo, useState } from 'react'

function Counter1({ data }) {
    console.log(data)
    // console.log(c)
    return (
        <>
        </>
    )
}

export default React.memo(Counter1);
